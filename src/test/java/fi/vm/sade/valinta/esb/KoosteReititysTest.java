package fi.vm.sade.valinta.esb;

import org.junit.Ignore;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

/**
 * 
 * @author Jussi Jartamo
 * 
 */
@Ignore
@ContextConfiguration(locations = "classpath:test-context.xml")
@RunWith(SpringJUnit4ClassRunner.class)
public class KoosteReititysTest {

    // @Autowired ValintalaskentaService valintalaskentaService;

    // @Autowired ValintaperusteService valintaperusteService;

    // @Autowired HakemusService hakemusService;

    @Test
    public void testKoosteReititys() {
        // valintalaskentaService.laske(hakukohdeOid, valinnanVaihe, hakemus,
        // valintaperusteet)
        // valintaperusteService.haeValintaperusteet(hakuparametrit)
        // valintaperusteService.haeValintatapajonotSijoittelulle(hakukohdeOid)

    }

}
